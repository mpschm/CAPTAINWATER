<div class="Players-ranking">

  <div
    class="bubbles"
    style="background-image: url(<%= image_path 'bubbles_image.png' %>)"
  >
    <div
      class="bubble1"
    >
      <div>
        <h1>Winner!</h1>
        <h1><strong><%= @play.first.user.pseudo %></strong></h1>
        <h1><%= @play.first.score %> points</h1>

      </div>

    </div>

      <div class="bubble2">
        <h1><%= @play.first.user.pseudo %>
        </br>
        <%= @play.first.score %></h1>
      </div>

  </div>

  <%= link_to 'Play again', profile_path(@play), class: 'button' %>

<!--     <button type="button" id="button1" > -->
<!--       Play again
    </button> -->

</div>
